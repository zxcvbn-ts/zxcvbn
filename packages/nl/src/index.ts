import commonWords from './commonWords'
import translations from './translations'

const userInputs: string[] = []

export default {
  dictionary: {
    userInputs,
    commonWords
  },
  translations,
}
